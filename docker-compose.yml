services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/schoolbooks.db
      - APP_ENV=development
    restart: unless-stopped
